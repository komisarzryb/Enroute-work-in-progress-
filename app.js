const screens=document.querySelectorAll(".screen");document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{screens.forEach(s=>s.classList.remove("active"));document.getElementById(b.dataset.screen).classList.add("active");if(b.dataset.screen==="map"){if(typeof initMap==="function"&&!mapInitialized)initMap();if(typeof refreshMap==="function")setTimeout(refreshMap,100)}});document.getElementById("snake").onclick=()=>alert("Snake — dodamy pełną minigrę w kolejnej wersji.");